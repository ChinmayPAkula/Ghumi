# Leave unset in dev — Vite proxies /api to localhost:8000 (see vite.config.ts).
# Set this in Vercel for production, pointing at the Railway backend URL.
VITE_API_BASE_URL=

# Supabase (if the frontend talks to Supabase directly for auth)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
