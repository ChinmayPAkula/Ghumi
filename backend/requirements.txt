# --- API framework ---
fastapi
uvicorn[standard]

# --- Agent orchestration ---
langgraph
langchain
langchain-groq

# --- Schema/validation ---
pydantic>=2
pydantic-settings

# --- Database / Auth ---
supabase
psycopg2-binary
sqlalchemy

# --- Observability ---
langsmith
sentry-sdk[fastapi]

# --- Env ---
python-dotenv

# --- External APIs ---
# Flights (Duffel) and hotels (Hotelbeds) are called directly via httpx, no
# SDK needed. Amadeus is not used — its self-service/test tier was
# decommissioned 2026-07-17 (Enterprise API only now).
googlemaps
httpx

# --- Testing ---
pytest
pytest-asyncio
