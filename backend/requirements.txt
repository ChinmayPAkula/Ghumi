# --- API framework ---
fastapi
uvicorn[standard]

# --- Agent orchestration ---
langgraph
langchain
langchain-groq

# --- Schema/validation ---
pydantic>=2
pydantic-settings

# --- Database / Auth ---
supabase
psycopg2-binary
sqlalchemy

# --- Observability ---
langsmith
sentry-sdk[fastapi]

# --- Env ---
python-dotenv

# --- External APIs ---
# Flights (Duffel), hotels (Hotelbeds), and food/activities (Google Places
# API New) are all called directly via httpx, no SDK needed. Amadeus is not
# used — its self-service/test tier was decommissioned 2026-07-17
# (Enterprise API only now). The legacy googlemaps SDK is not used either —
# its places()/geocode() methods hit the old Places API, which isn't
# enabled on an unbilled API key.
httpx

# --- Testing ---
pytest
pytest-asyncio
